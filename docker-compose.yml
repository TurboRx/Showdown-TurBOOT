services:
  bot:
    build: .
    container_name: showdown-turboot
    restart: unless-stopped
    env_file:
      - .env
    # Override or add individual environment variables here:
    # environment:
    #   PS_USERNAME: MyBot
    #   PS_ROOMS: botdevelopment,lobby
    stop_grace_period: 10s
